- hide command line when it's not focused (or just use a different text color)

- select pasted text
- each non-alphanumeric symbol should count as a word?
- position history?
- code folding?
- regexp?

- command to iterate through all the tokens of some type (make a separate command or shoehorn inside s?)

- bold matched text in autocompletions window and iopen window

